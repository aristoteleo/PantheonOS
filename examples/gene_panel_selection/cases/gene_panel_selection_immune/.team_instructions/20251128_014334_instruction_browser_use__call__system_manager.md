We need to use the web tools `duckduckgo_search` and `web_crawl`, but they error: ModuleNotFoundError: ddgs (for duckduckgo_search) and crawl4ai (for web_crawl). Please install and configure the required Python packages so that functions.web__duckduckgo_search and functions.web__web_crawl work. After installation, verify by running a simple search for 'TISCH2' and a crawl of 'http://tisch2.comp-genomics.org/'. Report back the results and any steps taken.