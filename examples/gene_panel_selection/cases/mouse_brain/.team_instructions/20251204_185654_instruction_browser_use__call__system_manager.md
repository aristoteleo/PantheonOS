Caller: selection_expert agent.

Task: Please install the Python package `adjustText` (latest stable) into the current environment used by our notebooks, so we can generate centroid labels with repel for UMAP figures. If not already present, ensure it is importable in Jupyter (pip install adjustText). No other changes required. Report back once done.