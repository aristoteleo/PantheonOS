Caller: selection_expert

Task: Please install Python packages for plotting intersections of 5 sets:
- Prefer: venn (https://pypi.org/project/venn/) which supports up to 6 sets
- Fallback: upsetplot

Environment: conda env currently running the Jupyter kernel used by the selection_expert agent. Ensure packages are installed into this environment so that the notebook can import them.

After installation, no further action is required.