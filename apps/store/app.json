{
  "id": "store",
  "name": "Store",
  "version": "0.11.0",
  "apiVersion": 2,
  "description": "Install apps from Discover, manage Git repositories, pull updates and contribute through pull requests.",
  "surface": "dom",
  "entry": {
    "frontend": "ui:store"
  },
  "placement": {
    "requires": [
      "dom"
    ]
  },
  "notes": "Frontend implementation lives in pantheon-ui (src/desktop/apps) until the UI phase moves it here; this manifest is the definition."
}
