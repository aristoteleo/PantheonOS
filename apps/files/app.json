{
  "id": "files",
  "name": "Files",
  "version": "0.6.4",
  "apiVersion": 2,
  "description": "The workspace file browser.",
  "surface": "dom",
  "entry": {
    "frontend": "ui:files"
  },
  "placement": {
    "requires": [
      "dom"
    ]
  },
  "notes": "Frontend implementation lives in pantheon-ui (src/desktop/apps) until the UI phase moves it here; this manifest is the definition.",
  "dependencies": {
    "file-manager": {
      "range": "*",
      "uses": [
        "fs@1"
      ]
    }
  },
  "actions": [
    {
      "name": "navigate",
      "description": "Show a folder in this existing window.",
      "params": {
        "path": "absolute folder path"
      }
    },
    {
      "name": "refresh",
      "description": "Re-read the current folder."
    },
    {
      "name": "select",
      "description": "Select an entry in the current folder.",
      "params": {
        "name": "entry name from desktop_read"
      }
    },
    {
      "name": "open",
      "description": "Open an entry; folders navigate in this window and files open in their app.",
      "params": {
        "name": "entry name from desktop_read"
      }
    },
    {
      "name": "delete",
      "description": "Delete the named entries in the current folder.",
      "params": {
        "name": "one entry name",
        "names": "or an array of entry names"
      }
    }
  ]
}
