{
  "id": "agent",
  "name": "Agent",
  "version": "0.6.4",
  "apiVersion": 2,
  "description": "The chat surface: talk to the team, watch threads.",
  "surface": "dom",
  "entry": {
    "frontend": "ui:agent"
  },
  "placement": {
    "requires": [
      "dom"
    ]
  },
  "notes": "Frontend implementation lives in pantheon-ui (src/desktop/apps) until the UI phase moves it here; this manifest is the definition."
}
